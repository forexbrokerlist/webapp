export const blogConfig = {
  // Determines if table of contents is enabled
  tableOfContents: {
    enabled: true,
  },

  // Determines if tools mentioned are enabled
  toolsMentioned: {
    enabled: true,
  },
}
